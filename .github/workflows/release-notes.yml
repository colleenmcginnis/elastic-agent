name: Generate release notes

on:
  workflow_dispatch:
    inputs:
      bc_commit_sha:
        description: 'Commit SHA for the BC'
        type: string
        required: true
      version:
        description: 'Version to be released (for example, `9.2.1`)'
        type: string
        required: true

jobs:
  build-release-notes:
    uses: colleenmcginnis/elastic-agent-changelog-tool/.github/workflows/generate-release-notes.yml@centralize-workflow
    with:
      product: Elastic Agent
      labels: docs,forwardport-main,release,skip-changelog,Team:Docs
      bc_commit_sha: ${{ inputs.bc_commit_sha }}
      version: ${{ inputs.version }}
      release_manager: ${{ github.triggering_actor }}
    permissions:
      deployments: write
      id-token: write
      contents: read
      pull-requests: write
